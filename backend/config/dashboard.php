<?php

return [
    /*
     * «Сейчас» для расчёта статусов актов. Выписка датирована будущим
     * (июль–август 2026), поэтому давность оплат считаем от даты её
     * формирования, а не от реального системного времени.
     */
    'reference_date' => env('REFERENCE_DATE', '2026-08-14'),

    /*
     * Оплата старше этого числа дней с незакрытым актом → «требует внимания».
     */
    'act_attention_days' => (int) env('ACT_ATTENTION_DAYS', 30),

    /*
     * Наш расчётный счёт в выписке. По нему парсер определяет направление
     * операции (наш счёт плательщик → списание, получатель → поступление).
     */
    'own_account' => env('OWN_ACCOUNT', '40802810937184056213'),
];
